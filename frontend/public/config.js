// Runtime configuration for production deployment
// Comment out API_URL to use localhost:3000 in local development
window.ENV = {
  // API_URL: 'https://crypto-crypto-backend.2msrpd.easypanel.host'
};
